<%= form_for @group, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :administrator, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :administrator, :class => 'check_box_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :editor, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :editor, :class => 'check_box_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :viewer, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :viewer, :class => 'check_box_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                groups_path, :class => 'btn' %>
  </div>
<% end %>
