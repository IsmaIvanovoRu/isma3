section.panel
  h1 Образовательные программы
  = link_to "Добавить", new_educational_program_path, class: 'button'
  ul
    - @educational_programs.each do |ep|
      .right
        - if @moderator_permission
          = link_to edit_educational_program_path(ep), title: 'Редактировать' do
            i.fi-page-edit
          = link_to ep, title: 'Удалить', method: :delete, data: { confirm: 'Are you sure?' } do
            i.fi-x
      li
        = link_to ep, title: ep.full_name do 
          = ep.full_name
          - if ep.adaptive
            span *

