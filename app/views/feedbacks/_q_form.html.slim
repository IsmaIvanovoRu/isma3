= form_for(@feedback) do |f|
  .field
    = f.text_area :question
  - unless controller_name == 'feedbacks'
    .field.hiden
      = f.check_box :private
  br
  .actions
    = f.submit 'Задать вопрос', class: "button small"
