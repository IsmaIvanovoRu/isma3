<%= link_to(image_tag(minify_img_path(image_attachment), size: dimension), "#", :title => image_attachment.title_sen_dot, :class => 'th left', "data-reveal-id" => "attachment_#{image_attachment.id}") %>
<div id="<%= "attachment_#{image_attachment.id}" %>" class="reveal-modal medium" data-reveal>
  <%= image_tag(inline_path(image_attachment)) %>
  <a class="close-reveal-modal">&#215;</a>
</div>